/**
 * Layouts Index - Page-level layout components
 *
 * These components define the overall application structure.
 * They compose feature components and connect to stores.
 *
 * @module layouts
 */

/** Root container wrapping the entire application */
export { AppShell } from "./AppShell";

/** Top toolbar with global actions */
export { Toolbar } from "./Toolbar";

/** Left configuration sidebar with test settings */
export { Sidebar } from "./Sidebar";

/** Right content area showing progress/results */
export { MainContent } from "./MainContent";

/** Bottom status bar with version and author info */
export { StatusBar } from "./StatusBar";
